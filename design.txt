
------------ Options Analysis -------------

-d dir[= /data]

COPY_DIR

-h

usage()

-n num[= 10]

COPY_NUM

-v

VERBOSE


------------ Basic Function -------------

0. config

Config() // ~/.afewmore/...
    > number of instance to create
    > created instance ids
    > copied instance ids

1. create COPY_NUM instances & fetch info

CreateNewInstance()
    > create instance of the same image-id
        > security-group
        > security-key
    > get
        > instance-id

2. copy data

GetInstanceList()
    > read config

CopyToInstance()
    > rsync -avr --progress -e "ssh -i us-east-1" data ubuntu@ip-172-31-46-157.ec2.internal:.

3. what next?


------------- Limitation --------------

* COPY_NUM
    > invalid number
    > very large
* COPY_DIR
    > path not exist? create
    > path with ' ' (spaces)
    > relative path? convert to canonical path
    > permission denied? sudo
    > data in path is large (>GB)? prompt this to user
    > or root path? prompt this to user
    > path encode problem ? [ignored]

